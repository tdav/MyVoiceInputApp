<Window x:Class="VoiceInputApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VoiceInputApp"
        mc:Ignorable="d"
        Title="Voice Input" Height="115" Width="240"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Border Background="#1E1E1E" CornerRadius="15" BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Верхняя панель управления -->
            <Grid Grid.Row="0" Margin="10,0">
                <Rectangle Width="35" Height="4" Fill="#444444" RadiusX="2" RadiusY="2"
                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
                
                <Button Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" Foreground="#888888"
                        Background="Transparent" BorderThickness="0" FontSize="10"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Click="CloseButton_Click" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Основной контент с иконками -->
            <Grid Grid.Row="1" Margin="15,0,15,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Кнопка Настройки -->

                <!-- Кнопка Микрофона (Центральная) -->
                <Button Grid.Column="1" x:Name="ControlButton" Click="ControlButton_Click"
                        Background="#2A2A2A" BorderThickness="0" Width="64" Height="64"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Ellipse x:Name="Circle" Fill="{TemplateBinding Background}"/>
                                <TextBlock x:Name="MicIcon" Text="&#xE720;" FontFamily="Segoe MDL2 Assets" 
                                           FontSize="28" Foreground="#FF8C69"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Circle" Property="Fill" Value="#353535"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Кнопка Помощь -->
            </Grid>
            
            <!-- Индикация статуса (скрытая, видна при наведении или работе) -->
            <TextBlock x:Name="StatusText" Text="Ready" Visibility="Collapsed"/>
            <TextBlock x:Name="ActivityText" Text="" Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
